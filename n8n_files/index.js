/** * N8N Main Entry Point * Starts the N8N workflow automation server */ const { N8N } = require('n8n');  async function start() {   try {     console.log('🚀 Starting N8N server...');     console.log(`📊 N8N Configuration:`);     console.log(`   - Host: ${process.env.N8N_HOST || '0.0.0.0'}`);     console.log(`   - Port: ${process.env.N8N_PORT || '5678'}`);     console.log(`   - Protocol: ${process.env.N8N_PROTOCOL || 'https'}`);     console.log(`   - Database: PostgreSQL`);      // Initialize N8N     const n8n = new N8N();      // Start the server     await n8n.start();     console.log('✅ N8N server started successfully!');     console.log(`🌐 Access N8N at: https://iqv2-n8n-bot.onrender.com`);   } catch (error) {     console.error('❌ Failed to start N8N:', error);     process.exit(1);   } }  // Handle graceful shutdown process.on('SIGTERM', async () => {   console.log('🛑 Received SIGTERM, shutting down gracefully...');   process.exit(0); });  process.on('SIGINT', async () => {   console.log('🛑 Received SIGINT, shutting down gracefully...');   process.exit(0); });  // Start the server start();